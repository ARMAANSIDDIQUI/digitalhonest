{
  "name": "digitalhonest-monorepo",
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "deploy-server": "npm --prefix server install"
  }
}
